using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MachinGunScript : MonoBehaviour
{
    private void Start()
    {
        
    }
    private void Update()
    {
        //Vector3 targetDirection = target.position = machineGun.position;

        //float speed = rotateSpeed * Time.deltaTime;

        //Vector3 newDirection = Vector3.RotateTowards(machineGun.forward, targetDirection, speed, 0f);

        //Debug.DrawRay(machineGun.position, newDirection, Color.red);

        //machineGun.rotation = Quaternion.LookRotation(newDirection);
    }
}
